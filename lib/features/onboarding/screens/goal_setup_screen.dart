import 'package:flutter/material.dart';

class GoalSetupScreen extends StatelessWidget {
  const GoalSetupScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Goal Setup')));
}
