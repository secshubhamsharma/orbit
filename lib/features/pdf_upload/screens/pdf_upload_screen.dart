import 'package:flutter/material.dart';

class PdfUploadScreen extends StatelessWidget {
  const PdfUploadScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('PDF Upload')));
}
